import fileObject from 'front-end/utils/file-object';
import { module, test } from 'qunit';

module('Unit | Utility | file object');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = fileObject();
  assert.ok(result);
});
